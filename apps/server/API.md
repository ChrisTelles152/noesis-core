# Server API Reference

> **This file has moved.** API documentation lives at
> [`docs/API_REFERENCE.md`](../../docs/API_REFERENCE.md) — the canonical
> integration reference for consumers of the Noesis engine (REST + WebSocket
> + SDK + data contracts + DB schema, all in one place).
>
> If you're updating an endpoint, update `docs/API_REFERENCE.md`. The
> live-running OpenAPI spec at `apps/server/openapi.ts` is the
> source-of-truth for the spec itself; the markdown reference is the
> human-readable explanation.
